<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tenlnet.model.mybatis.mapper.TestMapper">
<!--  新增记录  -->
    <insert id="insertTest" parameterType="com.tenlnet.model.mybatis.TestUser" useGeneratedKeys="true" keyProperty="testId">
        insert into test_user(name,phone,content) values(#{name},#{phone},#{content})
    </insert>
</mapper>          